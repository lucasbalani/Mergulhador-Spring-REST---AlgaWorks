package com.algaworks.api.domain;

//nao tem nada implementado pois esta interface so serve de marcação para o bean validation
public interface ValidationGroups {

		public interface ClienteId{ }
}
